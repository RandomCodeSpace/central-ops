name: CI Security Beta

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write
  security-events: write
  id-token: write
  actions: read

jobs:
  pipeline:
    uses: ./.github/workflows/reusable-pipeline.yml
    secrets: inherit
